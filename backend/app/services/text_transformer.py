def transform_text(text: str, reading_level: int) -> str:
    """
    Temporary transformation logic.
    Later this will be replaced with AI or rules.
    """
    return f"[LEVEL {reading_level}] {text}"
